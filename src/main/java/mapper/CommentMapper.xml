<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.CommentMapper">


    <insert id="addComment">
        insert into comment(comment_id, user_id, content, pid, createtime, updatetime, rating, state, username)
        values (#{commentId}, #{userId}, #{content}, #{pid}, #{createTime}, #{updateTime}, #{rating},
                #{state}, #{userName})
    </insert>

    <select id="queryByPid" resultType="pojo.Comment">
        select *
        from comment
        where pid = #{pid};
    </select>

</mapper>